{
  "name": "blockchain-nft-auth",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@alch/alchemy-web3": "^1.3.0",
    "@openzeppelin/contracts": "^4.5.0",
    "ganache": "^7.0.5",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "contract:deploy": "truffle compile && truffle migrate",
    "ganache": "ganache --server.host 127.0.0.1 --server.port 7545 --server.ws true --wallet.seed 19290209"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.3",
    "@testing-library/react": "^12.1.4",
    "@testing-library/user-event": "^13.5.0",
    "dotenv": "^16.0.0",
    "prettier": "^2.6.1"
  }
}
